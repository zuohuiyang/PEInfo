# 安全政策

## 支持范围

本项目为实验性工具，且为纯 vibe coding 产物；不承诺提供持续的安全修复或版本支持。

## 报告安全问题

如果你发现可能的安全漏洞（例如：解析恶意 PE 文件导致崩溃、越界读写、DoS 等），请避免公开披露细节，优先通过私信或私有渠道联系维护者。

在报告中请尽量提供：
- 复现步骤（最小化输入）
- 运行环境（Windows 版本、x86/x64、编译配置）
- 崩溃信息（调用栈/转储/日志）

## 使用建议

不要在生产环境或高权限环境中处理来自不可信来源的样本；建议在沙箱/隔离虚拟机中运行。
